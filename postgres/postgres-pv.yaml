apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pv-claim2
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
